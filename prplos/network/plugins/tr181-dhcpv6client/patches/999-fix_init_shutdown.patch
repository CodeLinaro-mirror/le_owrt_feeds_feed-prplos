Index: tr181-dhcpv6client-v1.2.1/scripts/tr181-dhcpv6client.sh
===================================================================
--- tr181-dhcpv6client-v1.2.1.orig/scripts/tr181-dhcpv6client.sh
+++ tr181-dhcpv6client-v1.2.1/scripts/tr181-dhcpv6client.sh
@@ -1,25 +1,30 @@
 #!/bin/sh
-[ -f /etc/environment ] && source /etc/environment
-ulimit -c ${ULIMIT_CONFIGURATION:-0}
+
+. /usr/lib/amx/scripts/amx_init_functions.sh
+
 name="tr181-dhcpv6client"
+datamodel_root="DHCPv6Client"
 
 case $1 in
-    start|boot)
-        ${name} -D
+    boot)
+        process_boot ${name} -D
         ;;
-    stop|shutdown)
-        if [ -f "/var/run/${name}.pid" ]; then
-            kill $(cat "/var/run/${name}.pid")
-        fi
+    start)
+        process_start ${name} -D
         ;;
-    debuginfo)
-        ubus-cli "DHCPv6Client.?"
+    stop)
+        process_stop ${name}
+        ;;
+    shutdown)
+        process_shutdown ${name}
         ;;
     restart)
         $0 stop
-        sleep 1
         $0 start
         ;;
+    debuginfo)
+        process_debug_info ${datamodel_root}
+        ;;
     *)
         echo "Usage : $0 [start|boot|stop|shutdown|debuginfo|restart]"
         ;;
