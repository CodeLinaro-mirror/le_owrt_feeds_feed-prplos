Index: tr181-qos-v3.3.12/scripts/tr181-qos.sh
===================================================================
--- tr181-qos-v3.3.12.orig/scripts/tr181-qos.sh
+++ tr181-qos-v3.3.12/scripts/tr181-qos.sh
@@ -1,27 +1,31 @@
 #!/bin/sh
-[ -f /etc/environment ] && source /etc/environment
-ulimit -c ${ULIMIT_CONFIGURATION:-0}
+
+. /usr/lib/amx/scripts/amx_init_functions.sh
+
+name="tr181-qos"
+datamodel_root="QoS"
 
 case $1 in
-    start|boot)
-	tr181-qos -D
+    boot)
+        process_boot ${name} -D
+        ;;
+    start)
+        process_start ${name} -D
         ;;
     stop)
-        if [ -f /var/run/tr181-qos.pid ]; then
-            kill `cat /var/run/tr181-qos.pid`
-        fi
+        process_stop ${name}
         ;;
-    debuginfo)
-	ubus-cli "QoS.?"
+    shutdown)
+        process_shutdown ${name}
         ;;
     restart)
         $0 stop
         $0 start
         ;;
-    log)
-	echo "TODO log qos manager"
-	;;
+    debuginfo)
+        process_debug_info ${datamodel_root}
+        ;;
     *)
-        echo "Usage : $0 [start|boot|stop|debuginfo|log]"
+        echo "Usage : $0 [start|boot|stop|shutdown|debuginfo|restart]"
         ;;
 esac
