Index: multisettings-v0.9.1/scripts/multisettings.sh
===================================================================
--- multisettings-v0.9.1.orig/scripts/multisettings.sh
+++ multisettings-v0.9.1/scripts/multisettings.sh
@@ -1,29 +1,31 @@
 #!/bin/sh
 
-source /etc/environment
+. /usr/lib/amx/scripts/amx_init_functions.sh
 
 name="multisettings"
+datamodel_root="MultiSettings"
 
 case $1 in
-    start|boot)
-	${name} -D
+    boot)
+        process_boot ${name} -D
         ;;
-    stop|shutdown)
-        if [ -f /var/run/${name}.pid ]; then
-            kill `cat /var/run/${name}.pid`
-        else
-            killall ${name}
-	fi
+    start)
+        process_start ${name} -D
         ;;
-    debuginfo)
-        ubus-cli "MultiSettings.?"
+    stop)
+        process_stop ${name}
+        ;;
+    shutdown)
+        process_shutdown ${name}
         ;;
     restart)
         $0 stop
         $0 start
         ;;
+    debuginfo)
+        process_debug_info ${datamodel_root}
+        ;;
     *)
         echo "Usage : $0 [start|boot|stop|shutdown|debuginfo|restart]"
         ;;
 esac
-
