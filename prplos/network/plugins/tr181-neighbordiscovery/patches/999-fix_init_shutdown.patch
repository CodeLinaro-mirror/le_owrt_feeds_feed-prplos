Index: tr181-neighbordiscovery-v0.8.2/scripts/tr181-neighbordiscovery.sh
===================================================================
--- tr181-neighbordiscovery-v0.8.2.orig/scripts/tr181-neighbordiscovery.sh
+++ tr181-neighbordiscovery-v0.8.2/scripts/tr181-neighbordiscovery.sh
@@ -1,23 +1,30 @@
 #!/bin/sh
 
+. /usr/lib/amx/scripts/amx_init_functions.sh
+
 name="tr181-neighbordiscovery"
+datamodel_root="NeighborDiscovery"
 
 case $1 in
-    start|boot)
-        ${name} -D
+    boot)
+        process_boot ${name} -D
         ;;
-    stop|shutdown)
-        if [ -f "/var/run/${name}.pid" ]; then
-            kill $(cat "/var/run/${name}.pid")
-        fi
+    start)
+        process_start ${name} -D
         ;;
-    debuginfo)
-        ubus-cli "NeighborDiscovery.?"
+    stop)
+        process_stop ${name}
+        ;;
+    shutdown)
+        process_shutdown ${name}
         ;;
-    restart|reboot)
+    restart)
         $0 stop
         $0 start
         ;;
+    debuginfo)
+        process_debug_info ${datamodel_root}
+        ;;
     *)
         echo "Usage : $0 [start|boot|stop|shutdown|debuginfo|restart]"
         ;;
