Index: tr181-syslog-v1.5.4/scripts/tr181-syslog.sh
===================================================================
--- tr181-syslog-v1.5.4.orig/scripts/tr181-syslog.sh
+++ tr181-syslog-v1.5.4/scripts/tr181-syslog.sh
@@ -1,27 +1,32 @@
 #!/bin/sh
 
-[ -f /etc/environment ] && source /etc/environment
-ulimit -c ${ULIMIT_CONFIGURATION:-0}
+. /usr/lib/amx/scripts/amx_init_functions.sh
 
 name="tr181-syslog"
+datamodel_root="Syslog"
 
 case $1 in
-    start|boot)
-        ${name} -D
+    boot)
+        process_boot ${name} -D
         ;;
-    stop|shutdown)
-        if [ -f "/var/run/${name}.pid" ]; then
-            kill $(cat "/var/run/${name}.pid")
-        fi
+    start)
+        process_start ${name} -D
         ;;
-    debuginfo)
-        ubus-cli "Syslog.?"
+    stop)
+        process_stop ${name}
+        ;;
+    shutdown)
+        process_shutdown ${name}
         ;;
-    restart|reboot)
+    restart)
         $0 stop
         $0 start
         ;;
+    debuginfo)
+        process_debug_info ${datamodel_root}
+        ;;
     *)
         echo "Usage : $0 [start|boot|stop|shutdown|debuginfo|restart]"
         ;;
 esac
+
