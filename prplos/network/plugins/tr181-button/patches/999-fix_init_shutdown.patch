Index: tr181-button-v0.0.3/scripts/tr181-button.sh
===================================================================
--- tr181-button-v0.0.3.orig/scripts/tr181-button.sh
+++ tr181-button-v0.0.3/scripts/tr181-button.sh
@@ -1,22 +1,30 @@
 #!/bin/sh
+
+. /usr/lib/amx/scripts/amx_init_functions.sh
+
 name="tr181-button"
+datamodel_root="Buttons.Button"
 
 case $1 in
-    start|boot)
-        ${name} -D
+    boot)
+        process_boot ${name} -D
         ;;
-    stop|shutdown)
-        if [ -f "/var/run/${name}.pid" ]; then
-            kill $(cat "/var/run/${name}.pid")
-        fi
+    start)
+        process_start ${name} -D
         ;;
-    debuginfo)
-        ubus-cli "Buttons.Button.?"
+    stop)
+        process_stop ${name}
+        ;;
+    shutdown)
+        process_shutdown ${name}
         ;;
     restart)
         $0 stop
         $0 start
         ;;
+    debuginfo)
+        process_debug_info ${datamodel_root}
+        ;;
     *)
         echo "Usage : $0 [start|boot|stop|shutdown|debuginfo|restart]"
         ;;
