Index: tr181-logical-v0.6.1/scripts/tr181-logical.sh
===================================================================
--- tr181-logical-v0.6.1.orig/scripts/tr181-logical.sh
+++ tr181-logical-v0.6.1/scripts/tr181-logical.sh
@@ -1,28 +1,31 @@
 #!/bin/sh
-[ -f /etc/environment ] && source /etc/environment
-ulimit -c ${ULIMIT_CONFIGURATION:-0}
+
+. /usr/lib/amx/scripts/amx_init_functions.sh
+
 name="tr181-logical"
+datamodel_root="Logical"
 
 case $1 in
-    start|boot)
-	${name} -D
+    boot)
+        process_boot ${name} -D
         ;;
-    stop|shutdown)
-        if [ -f /var/run/${name}.pid ]; then
-            kill `cat /var/run/${name}.pid`
-        else
-            killall ${name}
-	fi
+    start)
+        process_start ${name} -D
         ;;
-    debuginfo)
-        ubus-cli "Logical.?"
+    stop)
+        process_stop ${name}
+        ;;
+    shutdown)
+        process_shutdown ${name}
         ;;
     restart)
         $0 stop
         $0 start
         ;;
+    debuginfo)
+        process_debug_info ${datamodel_root}
+        ;;
     *)
         echo "Usage : $0 [start|boot|stop|shutdown|debuginfo|restart]"
         ;;
 esac
-
