OVERLAY_PATCHES := $(OVERLAY_PATCHES) $(wildcard $(THISDIR)/patches/*.patch)

define Package/$(PKG_NAME)/install-append
	cat $(THISDIR)/files/keepalived.init >> $$(1)/etc/init.d/keepalived
endef

$(call append,Package/$(PKG_NAME)/install,$(newline)$(Package/$(PKG_NAME)/install-append))
